package java24;

class Variable {
    public static void main(String[] args) {
        int tops;
        float gradePointAverage;
        char key ='C'; // A character is a single letter, number, puctuation mark, or symbol
        String productName = "Larvets"; // A string is a group of characters
        byte escapeKey = 27; // integer range from -128 to 127
        short roomNumber; // A short integer range from -32,768 to 32,767
        long salary = 264_400_000; // long integer range from -9.22 to 9.22 quintillion
        // ( _ ) can be use to break up lager number
        boolean gameOver = false; // can be used to store true or false values
        //naming convention in Java is to capitalize the name of variable that do not change in value
        final int TOUCHDOWN = 6;
        final int FIELDGOAL = 3;
        final int PAT = 1;
        // The F after the value 3.14 indicates that it's a float value
        float pi = 3.14F;
        // L is used for long and D is used for double
    }
}